import { View, Text } from "react-native";

export default function App() {
  return (
    <View
      style={{
        justifyContent: "center",
        alignItems: "center",
        flex: 1,
      }}
    >
      <Text>Welcome to my simple application</Text>
    </View>
  );
}
